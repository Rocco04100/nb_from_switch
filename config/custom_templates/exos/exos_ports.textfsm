Value PORT (\d+)
Value TYPE (\S+)

Start
  # Skip headers and start recording when a line begins with a number
  ^${PORT}\s+(?:\S+\s+)?${TYPE}\s+[A-Z]\s+[A-Z\d] -> Record
  # Ignore all other lines safely without crashing or ending early
  ^.*
